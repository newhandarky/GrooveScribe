# Refactoring work items

REFACTOR mode is optional and requires either an explicit behavior-preserving
refactoring request or a matching work item in `work-items/`.

Each work item should state:

- goal
- preserved external behavior
- affected symbols / execution flows
- test evidence
- completion evidence
